# Frontend API base URL.
# Local dev (with Vite proxy): /api
# Production (external backend): https://your-backend-domain.com/api
VITE_API_BASE=/api

# Optional: override local Vite proxy target for /api during npm run dev.
VITE_PROXY_TARGET=http://127.0.0.1:8000
